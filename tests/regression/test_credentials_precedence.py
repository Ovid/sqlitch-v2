"""Regression expectations for credential precedence resolution."""

from __future__ import annotations

import pytest


def test_cli_credential_precedence(tmp_path, monkeypatch: pytest.MonkeyPatch) -> None:
    pytest.fail("Pending implementation of CLI credential precedence resolution")
